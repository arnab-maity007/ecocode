# 🏗️ Architecture Overview

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND LAYER                          │
│  (React / Vue / Angular - Your Responsibility)                 │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐        │
│  │ Map Display  │  │ Risk Alerts  │  │ User Auth    │        │
│  │ (Leaflet/    │  │ Dashboard    │  │ Login/Signup │        │
│  │  Google Maps)│  │              │  │              │        │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘        │
│         │                  │                  │                 │
│         └──────────────────┼──────────────────┘                 │
│                            │                                     │
│                    HTTP/HTTPS Requests                          │
│                     (CORS Enabled)                              │
└────────────────────────────┼───────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FASTAPI BACKEND (main.py)                    │
│                                                                 │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │              API Routes & Endpoints                       │ │
│  │                                                           │ │
│  │  ┌─────────────────┐         ┌──────────────────┐       │ │
│  │  │  Floods Router  │         │   Auth Router    │       │ │
│  │  │  /api/v1/floods │         │  /api/v1/auth    │       │ │
│  │  │                 │         │                  │       │ │
│  │  │ • GET /floods   │         │ • POST /register │       │ │
│  │  │ • POST /floods  │         │ • POST /login    │       │ │
│  │  │ • GET /nearby   │         │ • GET /me        │       │ │
│  │  │ • POST /calc    │         │                  │       │ │
│  │  └────────┬────────┘         └────────┬─────────┘       │ │
│  │           │                           │                  │ │
│  └───────────┼───────────────────────────┼──────────────────┘ │
│              │                           │                     │
│              ▼                           ▼                     │
│  ┌─────────────────────────────────────────────────────────┐  │
│  │              Business Logic Layer                        │  │
│  │                                                          │  │
│  │  ┌──────────────────┐    ┌──────────────────┐          │  │
│  │  │ CRUD Operations  │    │   JWT Auth       │          │  │
│  │  │   (crud.py)      │    │   (utils/auth)   │          │  │
│  │  │                  │    │                  │          │  │
│  │  │ • Create event   │    │ • Hash password  │          │  │
│  │  │ • Read events    │    │ • Verify token   │          │  │
│  │  │ • Delete event   │    │ • Create token   │          │  │
│  │  └────────┬─────────┘    └──────────────────┘          │  │
│  │           │                                              │  │
│  │           ▼                                              │  │
│  │  ┌──────────────────────────────────────────┐           │  │
│  │  │    Flood Risk Service                    │           │  │
│  │  │    (services/flood_risk.py)              │           │  │
│  │  │                                           │           │  │
│  │  │  ┌─────────────────────────────────────┐ │           │  │
│  │  │  │   Risk Calculation Engine           │ │           │  │
│  │  │  │                                     │ │           │  │
│  │  │  │  Rainfall (0-60) + Elevation (0-40) │ │           │  │
│  │  │  │  = Risk Score (0-100)               │ │           │  │
│  │  │  │  → Severity (Low/Med/High/Critical) │ │           │  │
│  │  │  └─────────────────────────────────────┘ │           │  │
│  │  └────┬─────────────────────────────┬───────┘           │  │
│  │       │                             │                    │  │
│  └───────┼─────────────────────────────┼────────────────────┘  │
│          │                             │                       │
└──────────┼─────────────────────────────┼───────────────────────┘
           │                             │
           ▼                             ▼
┌───────────────────────┐    ┌───────────────────────┐
│  External APIs        │    │  Database Layer       │
│                       │    │  (PostgreSQL)         │
│  ┌─────────────────┐  │    │                       │
│  │ OpenWeatherMap  │  │    │  ┌─────────────────┐  │
│  │ • Current rain  │  │    │  │  flood_events   │  │
│  │ • Forecast      │  │    │  │  • id           │  │
│  └─────────────────┘  │    │  │  • location     │  │
│                       │    │  │  • lat/lng      │  │
│  ┌─────────────────┐  │    │  │  • risk_score   │  │
│  │ Google Elevation│  │    │  │  • severity     │  │
│  │ • Terrain height│  │    │  │  • rainfall_mm  │  │
│  │ • (or mock)     │  │    │  │  • elevation_m  │  │
│  └─────────────────┘  │    │  │  • timestamp    │  │
│                       │    │  └─────────────────┘  │
└───────────────────────┘    │                       │
                             │  ┌─────────────────┐  │
                             │  │  users          │  │
                             │  │  • id           │  │
                             │  │  • username     │  │
                             │  │  • email        │  │
                             │  │  • password     │  │
                             │  │  • is_active    │  │
                             │  └─────────────────┘  │
                             │                       │
                             └───────────────────────┘
```

## Request Flow Example

### Creating a Flood Event

```
1. Frontend User Action
   └─→ Click "Check Location" button with lat/lng

2. Frontend API Call
   └─→ POST /api/v1/floods/
       Body: { location_name, latitude, longitude }

3. FastAPI Router (floods.py)
   └─→ Validate request data with Pydantic schemas
   └─→ Call flood_risk_service.calculate_flood_risk()

4. Flood Risk Service
   ├─→ Call get_rainfall_data() → OpenWeatherMap API
   ├─→ Call get_elevation_data() → Google Elevation API
   ├─→ Calculate risk_score using formula
   │   • Rainfall contribution: 0-60 points
   │   • Elevation contribution: 0-40 points
   │   • Total: 0-100 scale
   └─→ Determine severity level

5. CRUD Operations (crud.py)
   └─→ Create FloodEvent model instance
   └─→ Save to PostgreSQL database

6. Return Response
   └─→ JSON response with all flood data
       {
         id, location_name, lat, lng,
         risk_score, severity,
         rainfall_mm, elevation_m,
         timestamp
       }

7. Frontend Updates
   └─→ Display marker on map
   └─→ Show risk alert if severity > Medium
   └─→ Update dashboard statistics
```

## Data Flow Diagram

```
External APIs                Backend Services           Database
─────────────               ─────────────────          ────────

OpenWeather ──rainfall──┐
                        │
Google Maps ─elevation──┤
                        │
                        ▼
                  Risk Calculator
                  (Algorithm MVP)
                        │
                        ├──→ Risk Score (0-100)
                        ├──→ Severity Level
                        └──→ Factor Breakdown
                              │
                              ▼
                        CRUD Operations
                              │
                              ├──→ CREATE → Insert ──→ PostgreSQL
                              ├──→ READ ──→ Select ──→ PostgreSQL
                              └──→ DELETE → Delete ─→ PostgreSQL
```

## Module Dependency Graph

```
main.py
  │
  ├─→ app/config.py (Settings)
  │     └─→ .env (Environment Variables)
  │
  ├─→ app/database.py (DB Connection)
  │     ├─→ app/config.py
  │     └─→ app/models.py (ORM Models)
  │
  ├─→ app/routers/floods.py
  │     ├─→ app/schemas.py (Validation)
  │     ├─→ app/crud.py (Database Ops)
  │     └─→ app/services/flood_risk.py
  │           ├─→ app/config.py (API Keys)
  │           └─→ External APIs
  │
  └─→ app/routers/auth.py
        ├─→ app/schemas.py
        ├─→ app/crud.py
        └─→ app/utils/auth.py (JWT)
              └─→ app/config.py
```

## Technology Stack

```
┌─────────────────────────────────────────┐
│           APPLICATION LAYER              │
│  • FastAPI (Web Framework)              │
│  • Pydantic (Data Validation)           │
│  • Python 3.9+                          │
└─────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────┐
│           BUSINESS LOGIC                │
│  • Risk Calculation Algorithm           │
│  • JWT Authentication                   │
│  • API Integration Logic                │
└─────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────┐
│           DATA ACCESS LAYER             │
│  • SQLAlchemy ORM                       │
│  • PostgreSQL Driver (psycopg2)         │
└─────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────┐
│           DATABASE                      │
│  • PostgreSQL 12+                       │
└─────────────────────────────────────────┘
                    │
┌─────────────────────────────────────────┐
│           EXTERNAL SERVICES             │
│  • OpenWeatherMap API                   │
│  • Google Elevation API                 │
└─────────────────────────────────────────┘
```

## Security Layers

```
┌──────────────────────────────────────────────────┐
│  1. Transport Layer Security (HTTPS/TLS)        │
│     • Encrypted communication                   │
└──────────────────────────────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  2. CORS Middleware                              │
│     • Allowed origins validation                │
└──────────────────────────────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  3. Input Validation (Pydantic)                  │
│     • Request schema validation                  │
│     • Type checking                              │
└──────────────────────────────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  4. JWT Authentication (Optional)                │
│     • Token-based auth                           │
│     • Password hashing (bcrypt)                  │
└──────────────────────────────────────────────────┘
                     ▼
┌──────────────────────────────────────────────────┐
│  5. Database Layer (SQLAlchemy)                  │
│     • SQL injection prevention                   │
│     • Parameterized queries                      │
└──────────────────────────────────────────────────┘
```

## Scaling Strategy

```
Current (MVP):
┌────────────┐    ┌──────────────┐    ┌──────────┐
│  Frontend  │───→│ FastAPI (1)  │───→│PostgreSQL│
└────────────┘    └──────────────┘    └──────────┘

Production:
┌────────────┐    ┌──────────────┐    ┌──────────────┐
│  Frontend  │───→│ Load Balancer│───→│ FastAPI (N)  │
│   + CDN    │    │   (Nginx)    │    │  Workers     │
└────────────┘    └──────────────┘    └──────┬───────┘
                                              │
                         ┌────────────────────┼────────────┐
                         ▼                    ▼            ▼
                    ┌─────────┐        ┌──────────┐  ┌────────┐
                    │ Redis   │        │PostgreSQL│  │External│
                    │ Cache   │        │  Primary │  │  APIs  │
                    └─────────┘        └────┬─────┘  └────────┘
                                            │
                                            ▼
                                       ┌──────────┐
                                       │PostgreSQL│
                                       │ Replica  │
                                       └──────────┘
```

## Future ML Integration

```
Current Risk Calculation:
  Rainfall + Elevation → Simple Formula → Risk Score

Future CNN-LSTM Model:
  ┌─────────────────────────────────────────────┐
  │  Input Features:                            │
  │  • Historical rainfall (time series)        │
  │  • Elevation data (spatial)                 │
  │  • Drainage capacity                        │
  │  • Soil permeability                        │
  │  • Urban density                            │
  └─────────────────┬───────────────────────────┘
                    ▼
  ┌─────────────────────────────────────────────┐
  │  CNN Layer (Spatial Feature Extraction)     │
  └─────────────────┬───────────────────────────┘
                    ▼
  ┌─────────────────────────────────────────────┐
  │  LSTM Layer (Temporal Pattern Recognition)  │
  └─────────────────┬───────────────────────────┘
                    ▼
  ┌─────────────────────────────────────────────┐
  │  Dense Layers (Risk Prediction)             │
  └─────────────────┬───────────────────────────┘
                    ▼
              Risk Score (0-100)
```

---

**This architecture is designed to be:**
- ✅ Modular and maintainable
- ✅ Scalable for production
- ✅ Easy to extend with new features
- ✅ Ready for ML model integration
- ✅ Frontend-agnostic (works with any UI)
